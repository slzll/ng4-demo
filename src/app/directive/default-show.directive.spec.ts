import { DefaultShowDirective } from './default-show.directive';

describe('DefaultShowDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultShowDirective();
    expect(directive).toBeTruthy();
  });
});
