import { DataSlideToDirective } from './data-slide-to.directive';

describe('DataSlideToDirective', () => {
  it('should create an instance', () => {
    const directive = new DataSlideToDirective();
    expect(directive).toBeTruthy();
  });
});
