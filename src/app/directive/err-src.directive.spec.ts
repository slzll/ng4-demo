import { ErrSrcDirective } from './err-src.directive';

describe('ErrSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrSrcDirective();
    expect(directive).toBeTruthy();
  });
});
